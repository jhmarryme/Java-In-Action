spring:
  application:
    name: feign-client

server:
  port: 40001
  servlet:
    context-path: /my-feign-client
eureka:
  client:
    service-url:
        defaultZone: http://127.0.0.1:20000/eureka