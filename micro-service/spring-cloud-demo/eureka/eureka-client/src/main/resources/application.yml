spring:
  application:
    name: eureka-client

server:
  port: 30000

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:20000/eureka
#  instance:
#    # 每隔5秒钟，向服务中心发送一条续约指令
#    lease-renewal-interval-in-seconds: 60
#    # 通知服务中心, 如果10秒内，依然没有收到续约请求，判定服务过期（上西天）
#    lease-expiration-duration-in-seconds: 5

#eureka:
#  client:
#    service-url:
#      # EurekaServer地址,多个地址以','隔开
##      defaultZone: http://peer1:20001/eureka
#      defaultZone: http://peer1:20001/eureka,http://peer2:20002/eureka
