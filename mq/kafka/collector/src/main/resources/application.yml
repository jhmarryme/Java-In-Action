server:
  port: 8001
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
spring:
  application:
    name: collector
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    default-property-inclusion: NON_NULL
    time-zone: GMT+8
