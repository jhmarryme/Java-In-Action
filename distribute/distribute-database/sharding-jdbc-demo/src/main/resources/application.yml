spring:
  profiles:
    active: sharding-snowflake
logging:
  pattern:
    dateformat: HH:mm:ss
mybatis:
  mapper-locations: /mybatis/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
